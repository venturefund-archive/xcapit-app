export class CustomValidatorErrors {
  static hasNumber = {
    hasNumber: true
  };

  static hasCapitalCase = {
    hasCapitalCase: true
  };

  static hasSmallCase = {
    hasSmallCase: true
  };

  static noPasswordMatch = { noPasswordMatch: true };
}
