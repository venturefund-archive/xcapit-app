import { HideReferralPipe } from './hide-referral.pipe';

describe('HideReferralPipe', () => {
  it('create an instance', () => {
    const pipe = new HideReferralPipe();
    expect(pipe).toBeTruthy();
  });
});
