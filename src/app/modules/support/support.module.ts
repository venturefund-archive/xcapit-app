import { NgModule } from '@angular/core';
import { SupportRoutingModule } from './support-routing.module';

@NgModule({
  declarations: [],
  imports: [SupportRoutingModule],
})
export class SupportModule {}
