import { StrategyNamePipe } from './strategy-name.pipe';

describe('StrategyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StrategyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
